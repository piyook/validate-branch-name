{
  "name": "validate-branch-name",
  "version": "1.0.0",
  "description": "validate local git branch name",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky",
    "validate-branch-name": "bash validate-branch-name.sh"
  },
  "author": "piyook",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
